# ignore .git and .cache folder and directories
.cache
.git*

# ignore all markdown files (md)
*.md

# ignore performance tests
performance-tests

# ignore docker compose
docker-compose.yml

*.log

# kubernetes files
k8s/
.*.yml

# Build artifacts (builder stage will recreate these)
dist/
coverage/

# Don't include node_modules in build context (npm ci will install them)
node_modules/
